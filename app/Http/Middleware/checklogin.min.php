<?phpnamespace App\Http\Middleware;use Closure;use Illuminate\Http\Request;class checklogin{ public function handle(Request $request, Closure $next) { if(session()->missing("LOGGED_IN")){ echo "you are not logged in"; return redirect("login");} return $next($request);}}